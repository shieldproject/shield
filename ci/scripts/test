#!/bin/bash

set -e

whoami

ls -la

export GOPATH=${PWD}/gopath
export PATH=${PATH}:${GOPATH}/bin

cd ${GOPATH}/src/${MODULE}

godep restore

go vet $(go list ./... | grep -v vendor)
go test -v ./...

