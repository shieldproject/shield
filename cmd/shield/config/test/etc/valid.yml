---
backend: first
backends:
  http://first: basic mytoken1
  http://second: ""
aliases:
  first: http://first
  second: http://second
properties:
  http://second:
    skip_ssl_validation: true
# This is a key that should go away if the file was saved, because it won't get
# pulled into the config. Used for save testing.
not_saved: true