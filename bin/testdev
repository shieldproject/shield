#!/bin/bash
PORT=8181
export SHIELD_API=http://127.0.0.1:${PORT}
export PATH=$(pwd):$(pwd)/bin:${PATH}
workdir=""

setup_workdir() {
  workdir=$(mktemp -d /tmp/shield.testdev.XXXXXXX)
  trap "rm -rf ${workdir}" EXIT QUIT INT TERM

  mkdir ${workdir}/etc
  mkdir ${workdir}/var
  ssh-keygen -t rsa -f ${workdir}/var/key -N '' >/dev/null
  #rm ${workdir}/var/key.pub
}

# If a work directory is not supplied as a parameter,
# create one.
if [ $# -lt 2 ]; then
    setup_workdir
else
    workdir="${2}"
fi

case "${1}" in
(shieldd)
  cat >${workdir}/etc/shieldd.conf <<EOF
---
port:          ${PORT}
database_type: sqlite3
database_dsn:  ${workdir}/var/shield.db
private_key:   ${workdir}/var/key
workers:       3
max_timeout:   10
web_root: ./webui
auth:
  api_tokens:
    test: LETMEIN
  basic:
    user: testuser
    password: testpass
#  oauth:
#    provider: github
#    key: Fill this in with the github Client ID after creating a dev oauth app
#    secret: Fill this in with the github secret after creating a dev oath app
#    base_url: http://localhost:8181
#    sessions:
#      type: sqlite3
#      dsn: ${workdir}/var/sessions.db
EOF

  if [[ ! -f "${workdir}/var/shield.db" ]]; then
    echo ">> Setting up SHIELD schema in var/shield.db"
    ./shield-schema -t sqlite3 -d "${workdir}/var/shield.db"
    echo
  fi

  echo ">> RUNNING SHIELDD"
  ./shieldd -c ${workdir}/etc/shieldd.conf --log-level debug
  echo
  echo "shieldd exited."
  echo "Press enter to close this session."
  read JUNK
  ;;
(shieldagent)
  cat >${workdir}/etc/shield-agent.conf <<EOF
---
authorized_keys_file: ${workdir}/var/key.pub
host_key_file: ${workdir}/var/key
listen_address: 0.0.0.0:5444
plugin_paths:
  - $(pwd)
EOF

  echo ">> RUNNING SHIELD AGENT"
  ./shield-agent -c ${workdir}/etc/shield-agent.conf --log-level debug
  echo
  echo "shield-agent exited."
  echo "Press enter to close this session."
  read JUNK
  ;;
("")
  tmux new-session \; set-option update-environment ' SHIELD_API' \; \
       new-window -n core ./bin/testdev shieldd ${workdir} \; \
       new-window -n core ./bin/testdev shieldagent ${workdir} \;
  ;;

(*)
  echo >&2 "USAGE: $0 [ACTION]"
  echo >&2 ""
  echo >&2 "Run components of a test/dev shield setup, on http://127.0.0.1:${PORT}"
  echo >&2 ""
  echo >&2 "Actions:"
  echo >&2 "  shieldd     Run SHIELD core daemon"
esac

# vim:ft=bash
